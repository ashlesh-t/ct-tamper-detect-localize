# Contributing

Contributions are welcome! Please follow these steps:

1. Fork the repository.
2. Create a new branch (`git checkout -b feature-name`).
3. Make your changes. If you're touching `prototype/ct-tampering-detector/pipeline/`,
   add or update tests under `tests/unit/` where practical (see
   [`GETTING_STARTED.md`](GETTING_STARTED.md)).
4. Run the test suite: `pytest tests/unit/`.
5. Commit your changes (`git commit -m 'Add new feature'`).
6. Push to the branch (`git push origin feature-name`).
7. Open a Pull Request.

## Questions

Dataset access, missing training code, or anything else: ashleshat5@gmail.com
