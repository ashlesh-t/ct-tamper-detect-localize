# App dependencies — mirrors prototype/ct-tampering-detector/requirements.txt,
# the authoritative list used by the Dockerfile. Kept identical here so
# `pip install -r requirements.txt` works from the repo root for local dev.
-r prototype/ct-tampering-detector/requirements.txt

# Test/dev tooling (see tests/unit/, .github/workflows/tests.yml)
pytest
pytest-cov
